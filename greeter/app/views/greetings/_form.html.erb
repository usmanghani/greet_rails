<% @greeting.greeting_recipients.build %>
<%= form_for @greeting do |f| %>
  <% if @greeting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@greeting.errors.count, "error") %> prohibited this greeting from being saved:</h2>

      <ul>
      <% @greeting.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label :text, "Greeting" %><br />
  <%= f.text_area :text, :rows => 3 %>


    <%= render :partial => 'recipient',
               :locals => { :form => f, :greeting_recipients => @greeting.greeting_recipients } %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
